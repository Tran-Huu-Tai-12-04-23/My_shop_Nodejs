{{!-- <div>
  <a
    style="color: rgb(46, 135, 171);
  font-size: 24px;"
    href="/"
  >Back</a>
  <h1>Cart store</h1>
  <h2 style="color: rgb(163, 51, 51);">Amount : -- {{countCart}} -- </h2>
  <div class="wrap__store">
    {{#each listItems}}
      <div class="item__cart__store">
        <div class="wrap__cart__store">
          <img src="{{this.link_image}}" alt="" />
          <h5>{{this.description}}</h5>
          <div class="type__cart">
            <span>Phân Loại hàng: </span>
            <span>Túi zip</span>
          </div>
          <span class="cost">${{this.cost}}</span>

          <div style="cursor: pointer; user-select: none"class="nav__count">
            <span class="icon__pre">-</span>
            <span class="count__product"></span>
            <span class="icon__next">+</span>
          </div>

          <div class="option__cart">
            <button
              data-id="{{this._id}}"
              class="btn__remove__cart"
            >Remove</button>
            <button>Buy</button>
          </div>
        </div>
      </div>
    {{/each}}
  </div>
</div>
<form method="POST" class="form__delete__item__instrore"></form>

<script>
  const btnRemoveCart = $('.btn__remove__cart') ;
  const formDelete = $('.form__delete__item__instrore') ;console.log(formDelete);
  btnRemoveCart.click( function () { 
    const idItem = $(this).data('id');
    formDelete.attr( "action",
  `/user/cart/store/delete/${idItem}?_method=DELETE`); formDelete.submit();
   } );

    const iconPre = $('.icon__pre');
    const iconNext = $('.icon__next');
    const countProcut = $('.count__product');
    const btnAddCart = $('.button__add__cart');

    var count = 1;
    countProcut.text(count);
    iconPre.click( function() {
        count = $(this).parent().children('.count__product').text(); 
        count --;
        if( count <= 0) {
            count = 1;
        }
        $(this).parent().children('.count__product').text(count);
    })

    iconNext.click(function() {
        count = $(this).parent().children('.count__product').text(); 
        count ++;
        $(this).parent().children('.count__product').text(count);
    })

    btnAddCart.click( function(e) {
        e.preventDefault();
    })

</script> --}}

<div class="wrap__store mlr_100">
  <div class="list__product">
    <div class="list__product__header">
      <ul class="list__product__header__nav">
        <li>Product</li>
        <li>Name</li>
        <li>Unit Price</li>
        <li>Quantity</li>
        <li>Total</li>
      </ul>

    </div>
    <div class="list__product__info">
      {{#each listItems}}
        <div class="list__product__info__item">
          <div class="list__product__info__item__img">
            <i
              class="bx bx-x icon__remove"
              data-id="{{this.productID._id}}"
            ></i>
            <img src="{{this.productID.link_image}}" alt="" />
          </div>
          <span
            class="list__product__info__item__name"
          >{{this.productID.nameproduct}}</span>
          <span class="list__product__info__item__cost">$
            {{this.productID.cost}}</span>
          <div class="list__product__info__item__amount">
            <span data-id="{{this.productID._id}}" class="icon__p">-</span>
            <span class="count__product">{{this.amount}}</span>
            <span data-id="{{this.productID._id}}" class="icon__n">+</span>
          </div>
          <span class="list__product__info__item__cost total__cost">$
            {{this.productID.cost}}</span>
        </div>
      {{/each}}
    </div>
  </div>
  <div class="list__product__header__detail">
    <h1>Cart Totals</h1>
    <ul>
      <li>
        <span>Subtotal</span>
        <span>$ {{sumTotals}}</span>
      </li>
      <li>
        <span>Shipping</span>
        <span>$ 24</span>
      </li>
      <li>
        <span>Total</span>
        <span>$ {{calTotals sumTotals}}</span>
      </li>
    </ul>

    <h5><span>Payable Total</span>
      <span>$ {{calTotals sumTotals}}</span></h5>
    <button class="list__product__header__detail__pay">
      Proceed to Checkout
    </button>
  </div>
</div>

<form method="POST" class="form__action"></form>